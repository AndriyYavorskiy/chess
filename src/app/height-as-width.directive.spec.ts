import { HeightAsWidthDirective } from './height-as-width.directive';

describe('HeightAsWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new HeightAsWidthDirective();
    expect(directive).toBeTruthy();
  });
});
